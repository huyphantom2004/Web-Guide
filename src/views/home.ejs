<!-- home.ejs -->
<section class="content">
  <h2 style="justify-content: center;">QUẢN LÝ CỬA HÀNG TIỆN LỢI HƠN</h2>
  <p style="font-size: 20px;width: 1000px;">“Chúng tôi là team 25. Chúng tôi cung cấp website quản lý cửa hàng chuyên nghiệp và uy tín, giúp bạn quản lý cửa hàng nhanh chóng, tiện lợi, nâng cao lợi thế cạnh tranh so với đối thủ chung ngành. Với đội ngũ là chuyên gia trong lĩnh vực, chúng tôi cam kết mang đến trải nghiệm tuyệt vời nhất. ”.
  <div class="home-image">
    <img src="image1.jpg" alt="Image 1" class="image-slide">
    <img src="image2.jpg" alt="Image 2" class="image-slide">
    <img src="image3.jpg" alt="Image 3" class="image-slide">
  </div>
</section>

<script>
  let currentIndex = 0;
  const images = document.querySelectorAll('.image-slide');
  const totalImages = images.length;

  function changeImage() {
      images[currentIndex].classList.remove('active');
      currentIndex = (currentIndex + 1) % totalImages;  // Di chuyển đến ảnh tiếp theo
      images[currentIndex].classList.add('active');
  }

  // Thay đổi ảnh mỗi 3 giây
  setInterval(changeImage, 3000);

  // Khởi tạo ảnh đầu tiên là active
  images[currentIndex].classList.add('active');
</script>

<style>
  * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
  }

  body {
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f0f0f0;
  }

  .home-image {
      width: 80%;
      height: 60vh;
      overflow: hidden;
      position: relative;
  }

  .image-slide {
      position: absolute;
      top: 0;
      left: 100%;
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: left 1s ease-in-out;
  }

  .image-slide.active {
      left: 0;
  }
</style>
